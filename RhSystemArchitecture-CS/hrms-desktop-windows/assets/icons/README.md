# App icons

Place platform launcher icons here (referenced by `Dioxus.toml`):

- `icon.ico` — Windows
- `icon.png` — Linux / iOS / Android (multiple resolutions generated by `dx`)

Icon art is intentionally omitted from this structure skeleton.
