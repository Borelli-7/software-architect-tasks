-- Placeholder initial migration for the notification service.
-- Real schema (notifications, templates) will be defined here; kept non-empty
-- so `sqlx::migrate!()` finds the migrations directory at compile time.
SELECT 1;
