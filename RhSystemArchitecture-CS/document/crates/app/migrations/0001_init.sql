-- Placeholder initial migration for the document service.
-- Real schema (document metadata) will be defined here; kept non-empty so
-- `sqlx::migrate!()` finds the migrations directory at compile time.
SELECT 1;
